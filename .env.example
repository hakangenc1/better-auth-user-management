# ============================================
# REQUIRED ENVIRONMENT VARIABLES
# ============================================

# Better Auth Secret Key
# Used for encrypting credentials, session management, and security tokens
# MUST be at least 32 characters long for security
# IMPORTANT: Change this to a strong random string in production!
# Generate with one of these commands:
#   - openssl rand -base64 32
#   - node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
BETTER_AUTH_SECRET=your-secret-key-here-change-in-production

# Better Auth Base URL
# The base URL where your application is hosted
# Used for callback URLs, redirects, and API endpoints
# Development: http://localhost:5173
# Production: https://yourdomain.com
BETTER_AUTH_URL=http://localhost:5173

# ============================================
# OPTIONAL ENVIRONMENT VARIABLES
# ============================================

# Node Environment
# Controls application behavior and security settings
# Values: development | production | test
# Set to 'production' for secure cookies and optimized performance
NODE_ENV=development

# Skip Setup Wizard (Development Only)
# Set to 'true' to bypass the setup wizard during development
# WARNING: Only use this for testing purposes
# The application will expect database configuration to already exist
# SKIP_SETUP=false

# ============================================
# DATABASE CONFIGURATION (Legacy)
# ============================================
# NOTE: With the new setup wizard, database configuration is stored
# in .data/config.json after completing the setup process.
# The DATABASE_URL variable below is only used if you're migrating
# from an older version or bypassing the setup wizard.

# Database URL (Legacy)
# Path to SQLite database file or PostgreSQL connection string
# SQLite example: ./data/auth.db
# PostgreSQL example: postgresql://user:password@localhost:5432/dbname
DATABASE_URL=./data/auth.db

# ============================================
# FRONTEND CONFIGURATION
# ============================================

# API Base URL (for Vite frontend)
# The base URL for API requests from the frontend
# Should match BETTER_AUTH_URL in most cases
VITE_API_URL=http://localhost:5173
